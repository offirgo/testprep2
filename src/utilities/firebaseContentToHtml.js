export default function firebaseContentToHtml(firebaseLessonContentMap) {
	let htmlContent = [];
	// arrange by big lesson and small lessson

	console.log(firebaseLessonContentMap);
	/*go over small lesson and check and show
    text
    video
    images
    question
show next and back for everything excetp for 1_1 and last_last*/
	return htmlContent;
}
